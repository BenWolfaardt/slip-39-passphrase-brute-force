# BIP-39 mnemonic (12, 15, 18, 21, or 24 words - for Trezor and most hardware wallets)
MNEMONIC="your 20-word bip39 mnemonic phrase goes here"

# Passphrase components for brute force combinations (comma or space separated)
# These will be concatenated without spaces: e.g. "bitcoin2024" from "bitcoin" + "2024"
WORDS="password secret wallet bitcoin crypto test 123 2024 hello world"

# Target Ethereum address (the address you know the correct passphrase unlocks)
TARGET_ADDRESS="0x1234567890abcdef1234567890abcdef12345678"

# RPC Configuration (for balance checking) - MUCH FASTER than Etherscan!
# Option 1: Use public RPC (no API key required, but may have rate limits)
# Leave RPC_PROVIDER and RPC_API_KEY empty to use public endpoints

# Option 2: Use private RPC provider with API key (recommended for high volume)
# RPC_PROVIDER=infura
# RPC_API_KEY=your_infura_project_id

# RPC_PROVIDER=alchemy  
# RPC_API_KEY=your_alchemy_api_key

# Option 3: Use custom RPC URL
# RPC_URL=https://your-custom-rpc-endpoint.com

# Legacy Etherscan API (deprecated in favor of RPC)
# ETHERSCAN_API_KEY=""
